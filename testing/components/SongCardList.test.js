import { render } from '@testing-library/react';

describe('SongCardList', () => {
  const renderSongCardList = (songs) => render(<SongCardList songs={songs} />);

  it('should render a SongCardList to the screen', () => {

  });
});